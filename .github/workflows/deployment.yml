name: Deployment pipeline
run-name: Deploy ${{ github.ref_name }} to RPI

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deployment
        run: curl "${{ secrets.RPI_DEPLOY_URL }}/${{ github.ref_name }}"
